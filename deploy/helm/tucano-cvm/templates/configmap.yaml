apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tucano-cvm.configMapName" . }}
  labels:
    {{- include "tucano-cvm.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

services:
  cvm_api:
    build: .
    container_name: cvm_api
    command: uvicorn app.main:app --host 0.0.0.0 --port 8007 --workers ${API_WORKERS:-2}
    ports:
      - "8007:8007"
    env_file:
      - .env
    volumes:
      - .:/app
